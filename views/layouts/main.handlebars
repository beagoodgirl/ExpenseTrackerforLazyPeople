<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/fonts/css/all.min.css">
    <link rel="stylesheet" href="/stylesheets/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/{{stylesheet}}">
    <title>{{#if title}}{{title}}{{else}}{{user.name}}的私房錢{{/if}}</title>
</head>
<body class="bg-light" data-bs-theme="light">
    <nav class="navbar navbar-expand-lg bg-light pb-1 pt-2 mt-0" data-bs-theme="light">
        <div class="container-fluid">
            <a class="navbar-brand" href="/entries">{{#if user}}{{user.name}}{{else}}我{{/if}}的私房錢</a>
            {{#if isAuthenticated}}
            <a href="/users/logout" class="btn btn-outline-dark btn-sm my-2 my-sm-0">Logout</a>
            {{/if}}
        </div>
    </nav>


    {{{body}}}    
    {{#if script}}
    <script src="/javascripts/{{script}}"></script>
    {{/if}}
    <script src="/javascripts/popper.min.js"></script>
    <script src="/javascripts/bootstrap.min.js"></script>
</body>
</html>